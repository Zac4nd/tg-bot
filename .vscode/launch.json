{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: TG Bot Locale",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "justMyCode": true,
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}